version: '2'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    #bundle exec unicorn_rails -c config/unicorn.rb -E development -D
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    tty: true
    stdin_open: true
